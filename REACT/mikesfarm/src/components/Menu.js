import React from 'react'

const Menu = (props) => {
    return(
        <div>
            <h1>This is the Menu Component. I will dynamically render menu items from the database </h1>
        </div>
    )
}